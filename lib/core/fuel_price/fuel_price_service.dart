/// Petrol price fetch service — scrapes Indian fuel price pages.
///
/// Fallback: if fetch fails, prompt user to enter today's rate manually.
/// Uses http + html packages for lightweight scraping.
class FuelPriceService {
  // TODO: Implement in Phase 1
  //
  // - HTTP fetch from fuel price API/page
  // - HTML parser for price extraction
  // - Cache last known price in SharedPreferences
  // - Fallback to manual entry UI
}
