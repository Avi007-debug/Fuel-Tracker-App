/// CSV exporter — raw data export for trips and fuel entries.
///
/// Uses the `csv` package for tabular data export.
class CsvExporter {
  // TODO: Implement in Phase 6 (v1.5)
  //
  // - Trips CSV (timestamp, route, distance, notes)
  // - Fuel entries CSV (timestamp, litres, price, mileage, cost)
  // - Share via share_plus
}
