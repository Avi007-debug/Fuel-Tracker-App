# Models Directory

This directory is intended for storing large model files (e.g., `.gguf` files like `qwen2.5-0.5b-instruct-q4_k_m.gguf`).

**Note:** Large model files are ignored by git to prevent blowing up the repository size. If you are setting up the project locally, please ensure you download the required models and place them in this folder.
